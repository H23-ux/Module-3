# Tuple in Python: Check Element Existence

## 🎯 Aim
To write a Python program that checks if the element `'n'` and the element `8` exist within a given tuple.

## 🧠 Algorithm
1. Define a tuple `x` with some letters and numbers.
2. Use the `in` operator to check if the string `'n'` exists within the tuple.
3. Use the `in` operator to check if the integer `8` exists within the tuple.
4. Print the results.

## 🧾 Program
```
x = ('a', 'b', 7, 'n', 'm', 10)

if 'n' in x:
    print("'n' exists in the tuple.")
else:
    print("'n' does not exist in the tuple.")

if 8 in x:
    print("8 exists in the tuple.")
else:
    print("8 does not exist in the tuple.")
```

## Output

<img width="368" height="140" alt="image" src="https://github.com/user-attachments/assets/a52f8a99-bab8-4ef5-9828-e98f3c0325c2" />

## Result
The Python program that checks if the element `'n'` and the element `8` exist within a given tuple has been written and the output is verified.
